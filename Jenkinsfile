pipeline {
  agent any
  stages {
    stage('variables') {
      agent { docker 'openjdk:7' }
      steps {
        sh "java -version"
      }
    }
    
    stage('variables') {
      agent { docker 'openjdk:7' }
      steps {
        sh "java -version"
      }
    }
  }
}
